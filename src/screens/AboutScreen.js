import React from 'react';

import {Text, View} from 'react-native';

export const AboutScreen = ({navigation}) => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>The most awesome app</Text>
    </View>
  );
};
